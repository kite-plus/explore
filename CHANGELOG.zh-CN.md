# 更新日志

本文件记录 Explore 的所有重要变更。

格式遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/spec/v2.0.0.html)。

[English](CHANGELOG.md)

## [未发布]

### 变更

- 页头把「最新」和「订阅」合并成一个「发现」：最新、推荐、订阅三个标签页都在它下面，共用标题「发现」，切换标签时只换下面一行说明和文章。
- 页脚的源代码链接改名为「GitHub」，前面加上 GitHub 图标。

## [0.1.4] - 2026-09-25

这一版打磨阅读页的细节：封面加载失败时不再露出破图，检测正常的文章显示绿色对号，点进博客的链接带上来源参数；站点地图补收的文章也修好了标题和日期。

### 新增

- 从 Explore 点进博客的链接（文章标题和博客页的「访问博客」）会带上 `utm_source=<本站域名>`，浏览器没有发送来源信息时，作者的统计也能看出访问来自 Explore。作者自己设置了 `utm_source` 的链接保持原样；订阅地址不加参数。

### 修复

- 封面图片加载失败（例如超过 2 MB）时，缩略图连同外框一起去掉，文章按没有封面的样子显示，不再出现破图。
- 链接检测正常的文章原来显示成米色圆底上的棕色对号，现在是绿色的圆圈对号，和其他状态的图标一致。
- 站点地图补收的 WordPress 文章，标题末尾不再带着博客名：比较博客名时弯引号和直引号视为相同，也会用页面的 `og:site_name`。这些文章也能读到发布日期了：读文章页时一直读到 `<body>`（最多 1 MB），并认 JSON-LD 里 WebPage 节点的日期。已经入库的文章会在升级后重新读取。

## [0.1.3] - 2026-09-25

Explore 不再只读订阅源：从文章页补上封面和摘要，从站点地图补收更早的文章。账号也补齐了：读者可以删除账号、修改密码，后台有了个人资料页，安装不再需要安装码。

### 新增

- 从站点地图补收更早的文章。订阅源只带最近的文章，更早的从 robots.txt 声明的、或常见位置的站点地图找出，按订阅源里文章链接的样子挑出文章地址，再从文章页 `<head>` 读出标题、发布时间、封面和摘要。每个博客最多 1000 篇，正文不读也不保存。
- 订阅源没给图片、或摘要被截断（例如 WordPress 的「[…]」）时，读一次文章页 `<head>` 里的 `og:image` 和 `og:description` 补上，并遵守页面里的 robots meta。
- 博客页的文章可以翻页，不再只显示最新的一批。
- 文章列表里显示博客的 favicon。
- 读者可以在账号页删除自己的账号；唯一的管理员不能删除。
- 可以修改密码：要先输入当前密码，改完后其他设备上的登录会失效，当前设备保持登录。
- 后台新增「个人资料」页，可以修改名称和密码。账号入口只留在侧边栏底部，头像是由邮箱决定颜色的名称首字母。
- 注册时要输入两次密码。

### 变更

- 前台的登录和注册页改成和后台登录页一样的版式。
- 安装向导不再要求安装码：还没有管理员时，第一个完成安装的人成为管理员，所以部署后要马上完成安装。
- 关于页的隐私说明补充了账号相关的内容。

## [0.1.2] - 2026-09-25

首页改成可以一直往前翻的完整时间流，并分成最新、推荐、订阅三个标签。

### 新增

- 首页分成「最新」「推荐」「订阅」三个标签。「最新」按发布时间倒序列出所有文章，同一个博客一天最多 3 篇，避免刷屏；「推荐」暂时是即将上线的说明；「订阅」是你订阅的博客的文章。
- 切换深浅色时，新主题从按钮处以圆形展开；系统要求减少动态效果时直接切换。

### 变更

- 首页不再只显示最近 30 天的文章，可以一直翻到最早。

## [0.1.1] - 2026-09-25

前台镜像只带运行所需的文件，体积明显变小。

### 变更

- `explore-web` 镜像不再带 `node_modules`：依赖在构建时打包进 `dist`，运行时只有 Node.js 和构建结果。

## [0.1.0] - 2026-09-25

Explore 的第一个版本：汇集独立博客公开的订阅源，按发布时间展示它们的文章。

### 新增

- 阅读：所有收录博客的文章汇成一条时间流，可以按语言和话题标签筛选；博客目录和每个博客的页面；`/feed.xml` 输出整条时间流，`/blogs.opml` 导出博客清单。界面有中文和英文，支持深色模式。
- 账号（可选）：订阅博客得到自己的阅读流，用 DNS TXT 记录认领自己的博客，在博客页报告问题。阅读不需要账号。
- 收录：作者提交博客时当场检查订阅源，不通过会说明要改什么；`explore check <url>` 在终端运行同样的检查。
- 抓取：遵守 robots.txt，使用条件请求，尊重 Retry-After；链接指向博客自身网站以外的文章会被丢弃；定时检测文章链接是否还能访问。
- 管理后台：审核投稿，管理博客和文章，处理下架申请和用户，查看抓取任务，开关注册、投稿和抓取。新安装由安装向导创建第一个管理员。
- 部署：每个版本发布 `ghcr.io/kite-plus/explore` 和 `ghcr.io/kite-plus/explore-web` 两个镜像（amd64、arm64），附 Docker Compose 和 Caddy 配置。
