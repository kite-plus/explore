---
import { loadBlog } from "@/lib/loaders";
import Blog from "@/views/Blog.astro";

const result = await loadBlog(Astro, "en");
---

<Blog lang="en" host={Astro.params.host ?? ""} result={result} />
